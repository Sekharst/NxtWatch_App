import styled from 'styled-components'

export const FontColorDiv = styled.div`
  color: ${props => (props.outline ? '#ffffff' : '#00070c1')};
  //   font-size: ${props => (props.outline ? '200px' : '20px')};
`
